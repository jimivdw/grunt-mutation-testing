<!DOCTYPE html>
<html>
    <head lang="en">
        <meta charset="UTF-8"/>
        <meta data-mutation-stats="{{{json stats}}}"/>
        <title>{{#if fileName}}{{fileName}} - {{/if}}Mutation test results</title>
        {{{style}}}
        {{{script}}}
    </head>

    <body>
        <header class="{{status}}">
            <h1>
                Mutation test results for
                <span class="fileName">
                    {{#if fileName}}
                        {{fileName}}
                    {{ else }}
                        All files
                    {{/if}}
                </span>
            </h1>

            <section id="mutationStats">
                <div class="stat">
                    Success rate:&nbsp;
                    <span class="statValue">
                        {{stats.successRate}}%&nbsp;
                        <small>
                            ({{stats.success}} / {{stats.all}})
                        </small>
                    </span>
                </div>
                <div class="stat">
                    Survived:&nbsp;
                    <span class="statValue">
                        {{stats.survivedRate}}%&nbsp;
                        <small>
                            ({{stats.survived}} / {{stats.all}})
                        </small>
                    </span>
                </div>
                <div class="stat">
                    Ignored:&nbsp;
                    <span class="statValue">
                        {{stats.ignoredRate}}%&nbsp;
                        <small>
                            ({{stats.ignored}} / {{stats.all}})
                        </small>
                    </span>
                </div>
                <div class="stat">
                    Untested:&nbsp;
                    <span class="statValue">
                        {{stats.untestedRate}}%&nbsp;
                        <small>
                            ({{stats.untested}} / {{stats.all}})
                        </small>
                    </span>
                </div>
            </section>
        </header>

        <nav>{{{breadcrumb}}}</nav>

        <main>
            {{{content}}}
        </main>

        <footer>
            <p>Report generated by <a class="brand" href="https://www.npmjs.com/package/grunt-mutation-testing">grunt-mutation-testing</a> on {{generatedAt}}.</p>
        </footer>
    </body>
</html>
